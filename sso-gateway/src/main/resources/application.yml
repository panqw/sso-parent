server:
  port: 9201

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone:  http://127.0.0.1:9100/eureka/


spring:
  application:
    name: gateway
  cloud:
    gateway:
         discovery:
               locator:
                 enabled: true
         routes:
         - id: oauth
           uri: lb://oauth
           predicates:
           - Path=/oauth/**
           filters:
           - StripPrefix=1
