<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sso.user.dao.UserMapper">

    <select id="findAllId" parameterType="int" resultType="com.sso.user.model.User" >
    select user.* , address.user_id,address.cityid from tb_user  user left join tb_address address  on user.id=address.user_id where user.id=#{user.id}
    </select>
</mapper>
