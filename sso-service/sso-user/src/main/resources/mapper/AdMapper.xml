<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sso.user.dao.AdMapper">

<!--  新增-->
    <insert id="saveAd" parameterType="com.sso.user.model.Ad">
        insert into Ad(id,name,position,startTime,endTime,status,image,url,remarks) value (#{id},#{name},#{position},#{startTime},#{endTime},#{status},#{image},#{url},#{remarks});
    </insert>
<!--  查询  -->

</mapper>
